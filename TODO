FEATURES:

    Add ! and () for conditional statements

    Add support for lists in Print


SMALL FEATURES:

    Fix mismatch in boolean statements (such as 'true & 5')

REFACTORING:

    Refactor VariableContainer to use instanceof instead of getType
